{
  "name": "lotus-insights-dashboard-root",
  "private": true,
  "engines": {
    "node": "^18 || ^20",
    "npm": ">=8"
  },
  "scripts": {
    "build": "cd lotus-insights && npm ci --no-audit --no-fund && npm run build",
    "vercel-build": "cd lotus-insights && npm ci --no-audit --no-fund && npm run build"
  }
}
