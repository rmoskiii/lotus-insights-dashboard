{
  "version": 2,
  "builds": [
    {
      "src": "lotus-insights/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "installCommand": "cd lotus-insights && npm ci --no-audit --no-fund",
  "build": {
    "env": {}
  }
}
